# Public URL of the deployed backend API — no trailing slash
# For local Docker Compose this is handled by the nginx proxy, leave blank
# For production deployment, set this to your Railway/Render backend URL
REACT_APP_API_URL=
